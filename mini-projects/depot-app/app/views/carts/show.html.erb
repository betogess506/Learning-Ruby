<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>

<h2>Your Pragmatic Cart</h2>
<ul>
  <% @cart.line_items.each do |item| %>
  <li><%= item.quantity.to_s + "x " if item.quantity > 1 %><%= item.product.title %></li>
  <% end %>
</ul>
<%= link_to 'Back to Store', store_path %>